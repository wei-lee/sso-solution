apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sso-solution.fullname" . }}-test-connection"
  labels:
    {{- include "sso-solution.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sso-solution.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
